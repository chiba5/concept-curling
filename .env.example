# 採点プロバイダ: openai | demo（未設定時はキーの有無で自動判定）
SCORING_PROVIDER=
# OpenAI（無くても demo フォールバックで動く）
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.2
# CPU の擬似遅延 ms（未設定時 1000..3000 のランダム。テストは 0）
CPU_DELAY_MS=
PORT=3000
