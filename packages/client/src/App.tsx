import { DEFAULT_CONFIG } from '@concept-curling/shared';

export function App() {
  return (
    <main>
      <h1>概念カーリング</h1>
      <p>v2 構築中 — 既定人数: {DEFAULT_CONFIG.playerCount}</p>
    </main>
  );
}
