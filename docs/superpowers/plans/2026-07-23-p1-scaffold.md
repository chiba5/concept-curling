# P1: monorepo 足場 + shared パッケージ Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** concept-curling v2 の monorepo 足場を作り、全パッケージが依存する shared（型・zod スキーマ・定数）と lint/test/CI/hooks を green の状態で確立する。

**Architecture:** npm workspaces monorepo（shared / server / client）。shared は TS ソースを直接 export し（ビルド不要）、server は tsup でバンドル、client は Vite がバンドルする。設計書: `docs/superpowers/specs/2026-07-23-v2-rewrite-design.md`

**Tech Stack:** Node 22 / TypeScript 5 / zod 3 / Express 4 + socket.io 4 / Vite + React / Vitest / ESLint 9 (flat) + Prettier / GitHub Actions

## Global Constraints

- 作業ブランチ: `feat/v2-rewrite`（`feat/online-conversion` から分岐。Task 1 で作成）
- Node `>=22`、全パッケージ `"type": "module"`（ESM）
- パッケージ名: `@concept-curling/shared` / `@concept-curling/server` / `@concept-curling/client`
- スコアの向き: **0 = 深い関連 / 100 = 無関係**。コメントにもこの向きを明記する
- `.env` は Read/Edit 禁止（Task 8 の hook で強制）
- コミットメッセージは Conventional Commits 風日本語（例: `feat: ...` `chore: ...`）+ 末尾に `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- 依存のバージョンは下記に「最低メジャー」を記す。インストール時は context7 等で現行安定版を確認し、メジャーが同じなら latest を使う
- シェルは Git Bash（Windows）。パスは forward slash

---

### Task 1: ブランチ作成と旧実装の撤去

**Files:**
- Delete: `server.js`, `public/`（3 ファイル）, `concept-online-min/`, `package.json`, `package-lock.json`
- 残す: `README.md`（P4 で書き直すまで旧内容のまま）, `CLAUDE.md`, `docs/`, `.gitignore`

**Interfaces:**
- Consumes: なし
- Produces: 空の作業ツリー（以降の全タスクの前提）

- [ ] **Step 1: ブランチ作成**

```bash
git checkout -b feat/v2-rewrite
```

- [ ] **Step 2: 旧実装を削除**

```bash
git rm server.js package.json package-lock.json
git rm -r public concept-online-min
```

- [ ] **Step 3: CLAUDE.md の冒頭に注意書きを挿入**

`CLAUDE.md` の 1 行目タイトルの直後に以下を挿入（§2〜§5 は旧実装の説明として残すが、実装参照させない）:

```markdown
> **⚠️ このブランチ（feat/v2-rewrite）は v2 全面作り直し作業中。**
> 以下の §2〜§5 は削除済みの旧実装の説明であり、現在のコードには存在しない。
> v2 の正は `docs/superpowers/specs/2026-07-23-v2-rewrite-design.md`。
> 検証コマンドはルートで `npm run check`（lint + typecheck + test + build）。
```

- [ ] **Step 4: コミット**

```bash
git add -A CLAUDE.md
git commit -m "chore: v2 作り直しのため旧実装を撤去"
```

---

### Task 2: ルート workspace とツール設定

**Files:**
- Create: `package.json`, `tsconfig.base.json`, `eslint.config.mjs`, `.prettierrc.json`, `vitest.workspace.ts`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: なし
- Produces: ルートコマンド `npm run lint` / `npm run typecheck` / `npm run test` / `npm run build` / `npm run check`（全タスク・CI が使用）

- [ ] **Step 1: ルート package.json を作成**

```json
{
  "name": "concept-curling",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace @concept-curling/server",
    "lint": "eslint .",
    "format": "prettier --write .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "build": "npm run build --workspaces --if-present",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "typescript-eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 2: tsconfig.base.json を作成**

```json
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "noEmit": true
  }
}
```

- [ ] **Step 3: eslint.config.mjs を作成**

```js
import tseslint from 'typescript-eslint';

export default tseslint.config(
  { ignores: ['**/dist/**', '**/node_modules/**', '.superpowers/**', 'docs/**'] },
  ...tseslint.configs.recommended,
  {
    rules: {
      '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
    },
  },
);
```

- [ ] **Step 4: .prettierrc.json を作成**

```json
{
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100
}
```

- [ ] **Step 5: vitest.workspace.ts を作成**

```ts
export default ['packages/*/vitest.config.ts'];
```

- [ ] **Step 6: .gitignore に追記**

既存の内容の末尾に追加:

```
dist/
coverage/
```

- [ ] **Step 7: インストールと検証**

```bash
npm install
npm run lint
```

Expected: lint がエラー 0 で終了（対象ファイルがまだ無くても正常終了すること）

- [ ] **Step 8: コミット**

```bash
git add -A
git commit -m "chore: monorepo 足場とツールチェーンを構築"
```

---

### Task 3: shared — 定数と型定義

**Files:**
- Create: `packages/shared/package.json`, `packages/shared/tsconfig.json`, `packages/shared/vitest.config.ts`, `packages/shared/src/index.ts`, `packages/shared/src/constants.ts`, `packages/shared/src/types.ts`

**Interfaces:**
- Consumes: なし
- Produces: **以下の型と定数は P2〜P4 の全コードが import する正本。名前・形を変える場合は必ずこのファイル経由で。**

- [ ] **Step 1: packages/shared/package.json を作成**

```json
{
  "name": "@concept-curling/shared",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
```

（ビルドスクリプトは持たない。TS ソースを直接 export し、server は tsup が、client は Vite がバンドルする）

- [ ] **Step 2: packages/shared/tsconfig.json を作成**

```json
{
  "extends": "../../tsconfig.base.json",
  "include": ["src", "vitest.config.ts", "test"]
}
```

- [ ] **Step 3: packages/shared/vitest.config.ts を作成**

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: { include: ['test/**/*.test.ts'] },
});
```

- [ ] **Step 4: src/constants.ts を作成**

```ts
import type { GameConfig } from './types.js';

/** ルーム設定の可変範囲 */
export const CONFIG_LIMITS = {
  playerCount: { min: 2, max: 6 },
  conceptsPerPlayer: { min: 3, max: 9 },
  themesCount: { min: 1, max: 4 },
  graceSeconds: { min: 10, max: 300 },
} as const;

/** ルーム作成フォームの初期値（現行ゲームと同一ルール） */
export const DEFAULT_CONFIG: GameConfig = {
  playerCount: 3,
  conceptsPerPlayer: 5,
  maxLives: 3,
  pickSumLimit: 150,
  destroyBand: { min: 10, max: 50 },
  themes: { count: 2, mode: 'llm' },
  graceSeconds: 60,
};

/** テーマ数から pickSumLimit の推奨初期値を出す（UI 用） */
export const suggestedPickSumLimit = (themeCount: number): number => themeCount * 75;

export const MAX_CONCEPT_LENGTH = 20;
export const MAX_NAME_LENGTH = 12;
export const ROOM_ID_LENGTH = 6;
export const ROOM_GC_MINUTES = 30;
```

- [ ] **Step 5: src/types.ts を作成**

```ts
/**
 * スコアの向き: 0 = 極めて深い関連 / 100 = 極めて浅い（無関係）。
 * 破壊判定: destroyBand.min <= score < destroyBand.max のライフが破壊される。
 */

export type Phase = 'waiting' | 'theming' | 'submitting' | 'picking' | 'battle' | 'finished';
export type Controller = 'human' | 'cpu';

export interface ThemeConfig {
  count: number;
  mode: 'llm' | 'manual';
  /** mode === 'manual' のとき count 個ちょうど */
  manual?: string[];
}

export interface GameConfig {
  playerCount: number; // 2..6
  conceptsPerPlayer: number; // 3..9
  maxLives: number; // 1..conceptsPerPlayer-1
  pickSumLimit: number; // 全テーマとのスコア合計の上限
  destroyBand: { min: number; max: number }; // min < max、いずれも 0..100
  themes: ThemeConfig;
  graceSeconds: number; // 切断から CPU 代打までの猶予
}

/** submitting フェーズで採点された自分の候補（PrivateView 用） */
export interface ScoredCandidate {
  concept: string;
  /** themes と同順。scores[i] は themes[i] との無関係度 */
  scores: number[];
  /** scores と同順の採点根拠（20 字程度） */
  reasons: string[];
  total: number; // scores の合計
  pickable: boolean; // total <= config.pickSumLimit
}

export interface PublicPlayer {
  seat: number; // 1..playerCount
  name: string;
  controller: Controller;
  connected: boolean;
  alive: boolean;
  /** 提出済みか等、フェーズ進行の待ち表示に使う */
  ready: boolean;
  lifeCount: number;
  livesPublic: string[]; // 公開ライフ（SECRET を除く残存分）
  secretRevealed: string | null; // 破壊等で公開された SECRET
  hasSecret: boolean; // SECRET が未破壊で存在するか
}

export interface TurnDetail {
  atkSeat: number;
  atkConcept: string;
  targetSeat: number;
  targetKind: 'normal' | 'secret';
  /** 未公開 SECRET は 'SECRET' に伏せる */
  targetLabel: string;
  score: number;
  reason: string;
  destroyed: boolean;
}

export interface TurnRecord {
  round: number;
  attacks: { seat: number; concept: string }[];
  details: TurnDetail[];
  destroys: { seat: number; kind: 'normal' | 'secret'; concept: string }[];
  reveals: { seat: number; concept: string }[];
  eliminatedSeats: number[];
}

export interface PublicState {
  roomId: string;
  phase: Phase;
  round: number; // battle 開始で 1、以降ターンごとに +1
  themes: string[]; // theming 完了までは []
  config: GameConfig;
  players: PublicPlayer[];
  turns: TurnRecord[];
  winnerSeat: number | null; // finished かつ全滅時は null
  hostSeat: number;
}

export interface PrivateView {
  seat: number;
  playerToken: string;
  candidates: ScoredCandidate[]; // submitting の採点完了後に入る
  myLives: { normals: string[]; secret: string | null; secretDestroyed: boolean };
  attackSubmitted: boolean;
}

export interface ErrorPayload {
  message: string;
}

/** socket.io の ack 応答（全 client→server イベント共通） */
export type Ack<T = undefined> = { ok: true; data: T } | { ok: false; message: string };

export interface RoomJoined {
  roomId: string;
  seat: number;
  playerToken: string;
}

export interface CreateRoomPayload {
  name: string;
  config: GameConfig;
}
export interface JoinRoomPayload {
  roomId: string;
  name: string;
  /** 再接続時のみ。一致する席があれば復帰 */
  playerToken?: string;
}
export interface SubmitConceptsPayload {
  concepts: string[]; // conceptsPerPlayer 個ちょうど
}
export interface PickLivesPayload {
  /** candidates のインデックス（pickable なもののみ、1..maxLives 個） */
  selectedIndices: number[];
  /** selectedIndices に含まれる candidates インデックス */
  secretIndex: number;
}
export interface AttackPayload {
  concept: string;
}

export interface ClientToServerEvents {
  'room:create': (p: CreateRoomPayload, cb: (res: Ack<RoomJoined>) => void) => void;
  'room:join': (p: JoinRoomPayload, cb: (res: Ack<RoomJoined>) => void) => void;
  'room:addCpu': (cb: (res: Ack) => void) => void;
  'room:start': (cb: (res: Ack) => void) => void;
  'game:submitConcepts': (p: SubmitConceptsPayload, cb: (res: Ack) => void) => void;
  'game:pickLives': (p: PickLivesPayload, cb: (res: Ack) => void) => void;
  'game:attack': (p: AttackPayload, cb: (res: Ack) => void) => void;
}

export interface ServerToClientEvents {
  state: (s: PublicState) => void;
  private: (v: PrivateView) => void;
  errorMsg: (e: ErrorPayload) => void;
}
```

- [ ] **Step 6: src/index.ts を作成**

```ts
export * from './constants.js';
export * from './types.js';
export * from './schemas.js';
```

（schemas.ts は Task 4 で作るため、このタスクの typecheck は Step 7 で index.ts から一時的に schemas 行を除いた状態で行っても良い。最終形は上記）

- [ ] **Step 7: 検証**

`src/index.ts` の schemas 行を一時コメントアウトして:

```bash
npm run typecheck --workspace @concept-curling/shared
```

Expected: エラー 0

- [ ] **Step 8: コミット**

```bash
git add packages/shared
git commit -m "feat(shared): ゲーム型定義と定数を追加"
```

---

### Task 4: shared — zod スキーマ（TDD）

**Files:**
- Create: `packages/shared/src/schemas.ts`, `packages/shared/test/schemas.test.ts`
- Modify: `packages/shared/src/index.ts`（schemas 行のコメントアウト解除）

**Interfaces:**
- Consumes: Task 3 の型・定数
- Produces: `gameConfigSchema`, `conceptSchema`, `nameSchema`, `createRoomSchema`, `joinRoomSchema`, `submitConceptsSchema`, `pickLivesSchema`, `attackSchema`（P3 のサーバが全 client→server メッセージ検証に使用）

- [ ] **Step 1: 失敗するテストを書く（test/schemas.test.ts）**

```ts
import { describe, expect, it } from 'vitest';
import {
  DEFAULT_CONFIG,
  attackSchema,
  gameConfigSchema,
  pickLivesSchema,
  submitConceptsSchema,
} from '../src/index.js';

describe('gameConfigSchema', () => {
  it('既定設定を受理する', () => {
    expect(gameConfigSchema.safeParse(DEFAULT_CONFIG).success).toBe(true);
  });
  it('人数 1 / 7 を拒否する', () => {
    expect(gameConfigSchema.safeParse({ ...DEFAULT_CONFIG, playerCount: 1 }).success).toBe(false);
    expect(gameConfigSchema.safeParse({ ...DEFAULT_CONFIG, playerCount: 7 }).success).toBe(false);
  });
  it('maxLives >= conceptsPerPlayer を拒否する', () => {
    expect(
      gameConfigSchema.safeParse({ ...DEFAULT_CONFIG, conceptsPerPlayer: 3, maxLives: 3 }).success,
    ).toBe(false);
  });
  it('destroyBand.min >= max を拒否する', () => {
    expect(
      gameConfigSchema.safeParse({ ...DEFAULT_CONFIG, destroyBand: { min: 50, max: 50 } }).success,
    ).toBe(false);
  });
  it('manual テーマの個数不一致を拒否する', () => {
    expect(
      gameConfigSchema.safeParse({
        ...DEFAULT_CONFIG,
        themes: { count: 2, mode: 'manual', manual: ['星座'] },
      }).success,
    ).toBe(false);
  });
  it('manual テーマ個数一致を受理する', () => {
    expect(
      gameConfigSchema.safeParse({
        ...DEFAULT_CONFIG,
        themes: { count: 2, mode: 'manual', manual: ['星座', '航海'] },
      }).success,
    ).toBe(true);
  });
});

describe('submitConceptsSchema', () => {
  it('空文字・空白のみの概念を拒否する', () => {
    expect(submitConceptsSchema.safeParse({ concepts: ['灯台', '  '] }).success).toBe(false);
  });
  it('21 文字の概念を拒否する', () => {
    expect(submitConceptsSchema.safeParse({ concepts: ['あ'.repeat(21)] }).success).toBe(false);
  });
  it('個数はスキーマでは検証しない（config 依存のためエンジンで検証）', () => {
    expect(submitConceptsSchema.safeParse({ concepts: ['灯台'] }).success).toBe(true);
  });
});

describe('pickLivesSchema', () => {
  it('secretIndex が selectedIndices に含まれない場合を拒否する', () => {
    expect(pickLivesSchema.safeParse({ selectedIndices: [0, 2], secretIndex: 1 }).success).toBe(
      false,
    );
  });
  it('secretIndex が selectedIndices 内なら受理する', () => {
    expect(pickLivesSchema.safeParse({ selectedIndices: [0, 2], secretIndex: 2 }).success).toBe(
      true,
    );
  });
  it('selectedIndices の重複を拒否する', () => {
    expect(pickLivesSchema.safeParse({ selectedIndices: [1, 1], secretIndex: 1 }).success).toBe(
      false,
    );
  });
  it('空の selectedIndices を拒否する', () => {
    expect(pickLivesSchema.safeParse({ selectedIndices: [], secretIndex: 0 }).success).toBe(false);
  });
});

describe('attackSchema', () => {
  it('前後空白をトリムして受理する', () => {
    const r = attackSchema.parse({ concept: ' 灯台 ' });
    expect(r.concept).toBe('灯台');
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npx vitest run packages/shared
```

Expected: FAIL（`schemas.js` が存在しない旨の import エラー）

- [ ] **Step 3: src/schemas.ts を実装**

```ts
import { z } from 'zod';
import { CONFIG_LIMITS, MAX_CONCEPT_LENGTH, MAX_NAME_LENGTH, ROOM_ID_LENGTH } from './constants.js';

/** 概念・名前に共通: トリム後 1 文字以上、制御文字なし */
const cleanString = (max: number) =>
  z
    .string()
    .transform((s) => s.trim())
    .pipe(
      z
        .string()
        .min(1)
        .max(max)
        // eslint-disable-next-line no-control-regex
        .regex(/^[^\u0000-\u001f\u007f]+$/),
    );

export const conceptSchema = cleanString(MAX_CONCEPT_LENGTH);
export const nameSchema = cleanString(MAX_NAME_LENGTH);

export const gameConfigSchema = z
  .object({
    playerCount: z
      .number()
      .int()
      .min(CONFIG_LIMITS.playerCount.min)
      .max(CONFIG_LIMITS.playerCount.max),
    conceptsPerPlayer: z
      .number()
      .int()
      .min(CONFIG_LIMITS.conceptsPerPlayer.min)
      .max(CONFIG_LIMITS.conceptsPerPlayer.max),
    maxLives: z.number().int().min(1),
    pickSumLimit: z.number().int().min(0),
    destroyBand: z.object({
      min: z.number().int().min(0).max(100),
      max: z.number().int().min(0).max(100),
    }),
    themes: z.object({
      count: z.number().int().min(CONFIG_LIMITS.themesCount.min).max(CONFIG_LIMITS.themesCount.max),
      mode: z.enum(['llm', 'manual']),
      manual: z.array(conceptSchema).optional(),
    }),
    graceSeconds: z
      .number()
      .int()
      .min(CONFIG_LIMITS.graceSeconds.min)
      .max(CONFIG_LIMITS.graceSeconds.max),
  })
  .refine((c) => c.maxLives < c.conceptsPerPlayer, {
    message: 'maxLives は conceptsPerPlayer 未満',
  })
  .refine((c) => c.destroyBand.min < c.destroyBand.max, { message: 'destroyBand は min < max' })
  .refine((c) => c.pickSumLimit <= c.themes.count * 100, {
    message: 'pickSumLimit はテーマ数 × 100 以下',
  })
  .refine((c) => c.themes.mode !== 'manual' || c.themes.manual?.length === c.themes.count, {
    message: 'manual テーマは count 個ちょうど',
  });

export const roomIdSchema = z
  .string()
  .length(ROOM_ID_LENGTH)
  .regex(/^[A-Z0-9]+$/);

export const createRoomSchema = z.object({ name: nameSchema, config: gameConfigSchema });
export const joinRoomSchema = z.object({
  roomId: roomIdSchema,
  name: nameSchema,
  playerToken: z.string().uuid().optional(),
});
export const submitConceptsSchema = z.object({ concepts: z.array(conceptSchema) });
export const pickLivesSchema = z
  .object({
    selectedIndices: z.array(z.number().int().min(0)).min(1),
    secretIndex: z.number().int().min(0),
  })
  .refine((p) => new Set(p.selectedIndices).size === p.selectedIndices.length, {
    message: 'selectedIndices が重複',
  })
  .refine((p) => p.selectedIndices.includes(p.secretIndex), {
    message: 'secretIndex は selectedIndices に含まれること',
  });
export const attackSchema = z.object({ concept: conceptSchema });
```

（`selectedIndices` の個数上限 `maxLives`・`concepts` の個数 `conceptsPerPlayer` は config 依存のため、スキーマではなく P2 エンジンで検証する）

- [ ] **Step 4: index.ts の schemas 行を有効化し、テストが通ることを確認**

```bash
npx vitest run packages/shared
npm run typecheck --workspace @concept-curling/shared
```

Expected: 全テスト PASS、typecheck エラー 0

- [ ] **Step 5: コミット**

```bash
git add packages/shared
git commit -m "feat(shared): client→server メッセージの zod スキーマを追加"
```

---

### Task 5: server スタブ（healthz + 静的配信）

**Files:**
- Create: `packages/server/package.json`, `packages/server/tsconfig.json`, `packages/server/vitest.config.ts`, `packages/server/src/index.ts`, `packages/server/src/app.ts`, `packages/server/test/app.test.ts`

**Interfaces:**
- Consumes: `@concept-curling/shared`（import できることの確認のみ）
- Produces: `createApp(): express.Express`（P3 が socket.io を被せる土台）、`npm run dev`（tsx watch）、`npm run build`（tsup → `dist/index.js`）

- [ ] **Step 1: packages/server/package.json を作成**

```json
{
  "name": "@concept-curling/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsup",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@concept-curling/shared": "*",
    "dotenv": "^16.0.0",
    "express": "^4.19.0",
    "socket.io": "^4.7.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^22.0.0",
    "@types/supertest": "^6.0.0",
    "supertest": "^7.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0"
  }
}
```

続けて `packages/server/tsup.config.ts` を作成（**shared は TS ソース直 export のため、必ずバンドルに含める**。含めないと本番の `node dist/index.js` が .ts を解決できず起動に失敗する）:

```ts
import { defineConfig } from 'tsup';

export default defineConfig({
  entry: ['src/index.ts'],
  format: 'esm',
  clean: true,
  noExternal: ['@concept-curling/shared'],
});
```

- [ ] **Step 2: tsconfig.json / vitest.config.ts を作成**

`packages/server/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "types": ["node"] },
  "include": ["src", "test", "vitest.config.ts"]
}
```

`packages/server/vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: { include: ['test/**/*.test.ts'] },
});
```

- [ ] **Step 3: 失敗するテストを書く（test/app.test.ts）**

```ts
import request from 'supertest';
import { describe, expect, it } from 'vitest';
import { createApp } from '../src/app.js';

describe('createApp', () => {
  it('GET /healthz が 200 と ok を返す', async () => {
    const res = await request(createApp()).get('/healthz');
    expect(res.status).toBe(200);
    expect(res.body).toEqual({ ok: true });
  });
});
```

- [ ] **Step 4: テストが失敗することを確認**

```bash
npx vitest run packages/server
```

Expected: FAIL（`app.js` が存在しない）

- [ ] **Step 5: src/app.ts と src/index.ts を実装**

`src/app.ts`:

```ts
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import express from 'express';
import { DEFAULT_CONFIG } from '@concept-curling/shared';

const clientDist = path.resolve(
  path.dirname(fileURLToPath(import.meta.url)),
  '../../client/dist',
);

export function createApp(): express.Express {
  const app = express();
  app.get('/healthz', (_req, res) => res.json({ ok: true }));
  // shared を実際に import しておく（配線検証。P3 で本格利用）
  app.get('/api/default-config', (_req, res) => res.json(DEFAULT_CONFIG));
  app.use(express.static(clientDist));
  // SPA fallback（/room/:id 直リンク用）
  app.get('*', (_req, res) => res.sendFile(path.join(clientDist, 'index.html')));
  return app;
}
```

`src/index.ts`:

```ts
import 'dotenv/config';
import http from 'node:http';
import { createApp } from './app.js';

const PORT = Number(process.env.PORT ?? 3000);
const server = http.createServer(createApp());
server.listen(PORT, () => console.log(`http://localhost:${PORT}`));
```

- [ ] **Step 6: テストとビルドが通ることを確認**

```bash
npx vitest run packages/server
npm run build --workspace @concept-curling/server
```

Expected: テスト PASS、`packages/server/dist/index.js` が生成される

- [ ] **Step 7: コミット**

```bash
git add packages/server
git commit -m "feat(server): Express スタブ（healthz・静的配信・SPA fallback）"
```

---

### Task 6: client スタブ（Vite + React）

**Files:**
- Create: `packages/client/package.json`, `packages/client/tsconfig.json`, `packages/client/vite.config.ts`, `packages/client/vitest.config.ts`, `packages/client/index.html`, `packages/client/src/main.tsx`, `packages/client/src/App.tsx`, `packages/client/test/App.test.tsx`

**Interfaces:**
- Consumes: `@concept-curling/shared`（DEFAULT_CONFIG の表示で配線確認）
- Produces: `npm run build` で `packages/client/dist/`（server が配信）。`App` コンポーネント（P4 で全面置換するスタブ）

- [ ] **Step 1: packages/client/package.json を作成**

```json
{
  "name": "@concept-curling/client",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build"
  },
  "dependencies": {
    "@concept-curling/shared": "*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "jsdom": "^25.0.0",
    "vite": "^6.0.0"
  }
}
```

- [ ] **Step 2: 設定ファイル 3 つを作成**

`packages/client/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "jsx": "react-jsx", "lib": ["ES2023", "DOM", "DOM.Iterable"] },
  "include": ["src", "test", "vite.config.ts", "vitest.config.ts"]
}
```

`packages/client/vite.config.ts`（開発時はサーバ 3000 へプロキシ）:

```ts
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:3000',
      '/socket.io': { target: 'http://localhost:3000', ws: true },
    },
  },
});
```

`packages/client/vitest.config.ts`:

```ts
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vitest/config';

export default defineConfig({
  plugins: [react()],
  test: { include: ['test/**/*.test.tsx'], environment: 'jsdom' },
});
```

- [ ] **Step 3: 失敗するテストを書く（test/App.test.tsx）**

```tsx
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { App } from '../src/App.js';

describe('App', () => {
  it('タイトルを表示する', () => {
    render(<App />);
    expect(screen.getByText('概念カーリング')).toBeTruthy();
  });
});
```

- [ ] **Step 4: テストが失敗することを確認**

```bash
npx vitest run packages/client
```

Expected: FAIL（App が存在しない）

- [ ] **Step 5: index.html / src を実装**

`packages/client/index.html`:

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>概念カーリング</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`src/App.tsx`:

```tsx
import { DEFAULT_CONFIG } from '@concept-curling/shared';

export function App() {
  return (
    <main>
      <h1>概念カーリング</h1>
      <p>v2 構築中 — 既定人数: {DEFAULT_CONFIG.playerCount}</p>
    </main>
  );
}
```

`src/main.tsx`:

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App.js';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

- [ ] **Step 6: 検証（テスト・ビルド・結合）**

```bash
npm install
npx vitest run packages/client
npm run build
node packages/server/dist/index.js & SERVER_PID=$!
npx wait-on -t 10000 http://localhost:3000/healthz
curl -s http://localhost:3000/healthz
curl -s http://localhost:3000/ | head -3
kill $SERVER_PID
```

Expected: テスト PASS、両ビルド成功、`{"ok":true}` と index.html の HTML が返る（`wait-on` は npx が一時取得するので依存追加は不要）

- [ ] **Step 7: コミット**

```bash
git add packages/client package-lock.json
git commit -m "feat(client): Vite + React スタブ"
```

---

### Task 7: GitHub Actions CI

**Files:**
- Create: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: ルートの `npm run check`
- Produces: push / PR ごとの CI（P2 以降は追加テストが自動で乗る）

- [ ] **Step 1: ci.yml を作成**

```yaml
name: CI
on:
  push:
    branches: ['main', 'feat/**']
  pull_request:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
```

- [ ] **Step 2: ローカルで check が通ることを確認**

```bash
npm run check
```

Expected: lint / typecheck / test / build すべて成功

- [ ] **Step 3: コミット**

```bash
git add .github
git commit -m "ci: lint・typecheck・test・build の GitHub Actions を追加"
```

（push は行わない。CI の実走確認はユーザーが push したタイミングで行う）

---

### Task 8: Claude Code hooks（.env ブロック + 自動整形）

**Files:**
- Create: `.claude/settings.json`, `.claude/hooks/block-env.mjs`, `.claude/hooks/format.mjs`

**Interfaces:**
- Consumes: なし
- Produces: 以降の全セッションで `.env` アクセス遮断・編集後 Prettier 自動整形

- [ ] **Step 1: .claude/hooks/block-env.mjs を作成**

```js
import { readFileSync } from 'node:fs';

const input = JSON.parse(readFileSync(0, 'utf8'));
const p = String(input.tool_input?.file_path ?? '').replaceAll('\\', '/');
if (/(^|\/)\.env($|\.)/.test(p)) {
  console.error('.env へのアクセスはプロジェクト方針でブロックされています');
  process.exit(2); // exit 2 = ツール呼び出しをブロック
}
```

- [ ] **Step 2: .claude/hooks/format.mjs を作成**

```js
import { execFileSync } from 'node:child_process';
import { existsSync, readFileSync } from 'node:fs';

const input = JSON.parse(readFileSync(0, 'utf8'));
const p = String(input.tool_input?.file_path ?? '');
if (p && /\.(ts|tsx|mjs|json|css|html)$/.test(p) && existsSync(p)) {
  try {
    execFileSync('npx', ['prettier', '--write', p], { stdio: 'ignore', shell: true });
  } catch {
    // 整形失敗はセッションを止めない
  }
}
```

- [ ] **Step 3: .claude/settings.json を作成**

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Edit|Write",
        "hooks": [{ "type": "command", "command": "node .claude/hooks/block-env.mjs" }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "node .claude/hooks/format.mjs" }]
      }
    ]
  }
}
```

- [ ] **Step 4: 動作確認**

hook は次セッションから有効。このタスク内では構文検証のみ:

```bash
echo '{"tool_input":{"file_path":".env"}}' | node .claude/hooks/block-env.mjs; echo "exit=$?"
echo '{"tool_input":{"file_path":"README.md"}}' | node .claude/hooks/block-env.mjs; echo "exit=$?"
```

Expected: 1 行目 `exit=2`（ブロック）、2 行目 `exit=0`（通過）

- [ ] **Step 5: コミット**

```bash
git add .claude
git commit -m "chore: .env ブロックと自動整形の Claude hooks を追加"
```

---

## 完了条件（P1 全体）

- `npm run check` がルートで全て green
- `curl http://localhost:3000/healthz` → `{"ok":true}`、`/` で client の HTML が返る
- shared の型・スキーマが本プランの記載どおり存在する（P2〜P4 の前提）
